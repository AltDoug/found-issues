# found-issues

Issues noticed outside the current task scope. Format: `- [status] YYYY-MM-DD path:line — symptom (suggested: fix)`. Statuses: `open`, `deferred`, `fixed`.

Maintained automatically by Claude. See <https://github.com/AltDoug/found-issues> for docs.

- [fixed] 2026-05-08 docs/demo-storyboard.md — gh repo create --push appears before source file creation, so the command fails on first run; reorder steps so commit happens before gh repo create (suggested: rewrite the bash setup block in the order: mkdir+cd, git init, mkdir src/docs + echo source, git add+commit, then gh repo create --source . --push) (PR: AltDoug/found-issues#11) (fixed: 2026-05-08)
- [fixed] 2026-05-09 bin/found-issues:1033 — install-statusline segment block calls 'found-issues status' without cd to workspace dir; fails on multi-line statuslines that use 'git -C $DIR' pattern (suggested: extract workspace dir from input JSON via jq, cd before invoking, or add --repo-dir flag to status subcommand) (PR: AltDoug/found-issues#37) (fixed: 2026-05-10)
- [fixed] 2026-05-09 .github/workflows/test.yml — no CI guard against non-ASCII test names; bats on Git Bash can't parse multi-byte UTF-8 in @test names and silently drops the test, which only manifests as a 'expected N got N-1' Windows runner failure (suggested: add a lint step using rg pattern '@test ".*[^[:ascii:]]' tests/ that fails the build with a clear error) (PR: AltDoug/found-issues#40) (fixed: 2026-05-10)
- [fixed] 2026-05-10 commands/uninstall.md — users naturally run `/plugin uninstall found-issues` (Claude Code platform command) instead of `/found-issues:uninstall` (our skill). The platform command does NOT invoke our cleanup → legacy statusline snippet, /fi alias, mode cache all survive. v1.0.4 install-statusline self-heal mitigates the legacy-snippet symptom on next install but discoverability gap is real (suggested: README + uninstall.md call out the order more loudly: 'run /found-issues:uninstall BEFORE /plugin uninstall'; or hook into a pre-uninstall lifecycle event if Claude Code adds one) (PR: AltDoug/found-issues#50) (fixed: 2026-05-10)
- [deferred] 2026-05-10 bin/found-issues:1166 cmd_install_statusline — has no awareness of whether ~/.claude/statusline.sh is the canonical machine file or a render target of an external sync system (e.g. dotfiles, chezmoi, yadm, a config sync layer). When it's a render target, install-statusline succeeds but the next sync overwrites the canonical block — silent regression. Hit during v1.0.4 dogfood: a config-management repo renders claude-only/statusline.sh to ~/.claude/statusline.sh, so every install-statusline migration got reverted on next config sync. Public users without a sync layer don't hit this, but the failure mode is invisible to the maintainer (counter just stops working again, no signal that an external system is fighting the install). (suggested: install-statusline could detect common render-target signals — symlink target outside HOME, sibling .gitignore'd file with same name, presence of a sync manifest in HOME — and emit a one-line warning at the end: 'note: ~/.claude/statusline.sh appears to be managed by <X>; if it gets reverted on next sync, add `found-issues install-statusline` as a post-render hook in your sync flow'. Low priority — only matters for power users with config management. Defer until second user reports the regression.) (reason: wait for second user report per entry guidance)
- [fixed] 2026-05-11 install-flow — /plugin install uses SSH clone, fails for users without SSH key (suggested: marketplace mitigated via AltDoug/claude-plugins#11; file upstream Claude Code installer bug) (PR: AltDoug/claude-plugins#11) (fixed: 2026-05-11)
- [fixed] 2026-05-12 commands/setup.md:94 — statusline detection only checks ~/.claude/statusline.sh; misses users with custom statusline configured via settings.json statusLine.command. Symptom: setup tells user "you're using Claude Code's default" even when they have a working custom statusline. (suggested: read settings.json + settings.local.json for statusLine.command; if non-convention path, surface manual-integration message) (PR: AltDoug/found-issues#78) (fixed: 2026-05-12)
- [fixed] 2026-05-12 hooks/stop-reminder.sh — Stop hook is not firing across any inspected transcript. Empirical sweep 2026-05-12: transcript A, a Bash-heavy work repo (2.5MB, extensive Bash tool use) → 0 hook-written `<!-- found-issues-checked: ... -->` markers; transcript B, a config-management repo (2.6MB, 32 substantive tool uses, NO project-level settings.json) → 0 markers; found-issues repo transcript (4.6MB, this session) → 0 hook-written markers (initial "8 hits" was false positive — all 8 are prose mentions from the diagnostic itself, not actual hook writes). Auto-logging in two of my non-found-issues repos dried up ~2026-05-04 despite plugin installed and configured correctly. Hook is declared in `hooks/hooks.json` Stop array; SessionStart hook works (sync runs at session start; statusline injection works), but the Stop event appears to never invoke the plugin's hook. Initial hypothesis about project-level `.claude/settings.json` interference was invalidated by transcript B evidence (no project settings, same gap). PostToolUse hooks may also be affected — annotation prompts after `gh pr create` / `git commit` have not been observed firing recently either. (suggested: instrument stop-reminder.sh with a debug log on entry [`echo "$(date) $$ fired" >> ~/.cache/found-issues/stop-debug.log`]; run a multi-turn session in any of the affected repos; if the file stays empty, the hook is never invoked → upstream Claude Code bug, file with repro; if the file fills but markers still don't appear, the hook fires but stderr-to-model surfacing is broken → plugin-side issue in how the marker requirement gets to Claude. Reproducer for upstream: any session in any repo with v1.2.0 plugin installed; no project-level config needed.) (PR: AltDoug/found-issues#83) (fixed: 2026-05-12)
- [fixed] 2026-05-12 lib/parse-entries.sh:73 — fi_parse_entry uses greedy `sed -E 's/^.*[0-9]{4}-[0-9]{2}-[0-9]{2} //'` to skip past the entry leading date, so when a symptom contains additional ISO dates (e.g. "Empirical sweep 2026-05-12... dried up ~2026-05-04"), after_date ends up starting somewhere inside the symptom text instead of the location. annotate-pr / annotate-commit then fail to extract the entry real path and silently report "no [open] entries match". Discovered while annotating PR #83 against an entry whose symptom contained two dates. (suggested: anchor the strip to the status-prefix pattern instead of `.*` — e.g. `sed -E 's/^- \[(open|deferred|fixed)\]( \[!\])? [0-9]{4}-[0-9]{2}-[0-9]{2} //'` — so only the leading entry-date is consumed; add a parse-entries test that feeds a multi-date symptom and asserts path extraction is still correct) (PR: AltDoug/found-issues#84) (fixed: 2026-05-12)
- [fixed] 2026-05-12 hooks/stop-reminder.sh:84 — `awk` invocation in the `last_turn` extractor fails with `awk: towc: multibyte conversion failure` when the transcript tail contains multibyte UTF-8 (em-dashes —, check marks like the heavy-check ✓ and white-heavy-check, smart quotes, etc.). Reproduced 2026-05-12 in a parallel Claude Code session whose response contained em-dashes and check marks; awk choked and the Stop hook surfaced the raw towc error to the model. Likely cascading: if awk fails, last-turn extraction is empty/garbled, the marker-presence check can't run correctly, and the hook may be silently failing across many sessions whose AI output contains common Unicode. Related to the #83 hook-not-firing investigation but a distinct failure mode (the hook IS firing here — it's failing inside awk rather than not running at all). (suggested: prefix the awk invocation with `LC_ALL=C.UTF-8 awk ...` so awk treats input bytes correctly — `C.UTF-8` is portable across BSD awk on macOS and gawk on Linux/Git Bash on Windows; audit other awk calls in `hooks/` for the same exposure. Alternative: replace this awk block with a python or shell-only last-turn extractor that handles UTF-8 by default. Add a regression test that feeds a transcript-tail containing an em-dash and a heavy-check character and asserts the hook still extracts the last turn and exits 0.) (PR: AltDoug/found-issues#89) (fixed: 2026-05-12)
- [fixed] 2026-05-13 bin/found-issues fi_strip_target_markers ~1982-1989 — two dead-code sub() regexes (\$\{__fiSeg\([^)]*\)\)\} and {_fi_seg\([^)]*\)\)}) cannot match v1.5.0 splice forms because the ternary args contain ) characters that terminate [^)]* early. Non-functional today (strip is only invoked on v1.4.x migration, which uses the bare value form handled by the working sub() lines below), but confusing for future maintainers (suggested: delete the dead patterns OR add inline comment noting they're for a hypothetical intermediate splice form that never shipped) (PR: AltDoug/found-issues#92) (fixed: 2026-05-15)
- [fixed] 2026-05-13 bin/found-issues cmd_doctor runtime probe ~2841 — probe_stdin JSON has no escaping for probe_cwd; if cwd contains a double-quote or backslash the synthetic stdin is malformed and the probe reports FAIL for a working integration. Low real-world impact (typical paths have neither character) but trivially exploitable via mkdir of an unusual path (suggested: pipe through jq -Rs or escape \\ and \" before substituting) (PR: AltDoug/found-issues#92) (fixed: 2026-05-15)
- [fixed] 2026-05-13 bin/found-issues runtime probe sub-probe (c) ~2859 — false-positive runtime-error detection via grep -qiE '(error|exception|traceback|undefined|cannot|not found)' against combined stderr+stdout. A legitimate statusline output containing 'undefined' (e.g. detached HEAD with branch=undefined) or path tokens like 'cannot-wait' would trigger a false FAIL diagnosis (suggested: scope detection to stderr only by separating capture, OR tighten the regex to error-message-shape patterns like ^Error:, TypeError, etc. rather than bare keywords) (PR: AltDoug/found-issues#92) (fixed: 2026-05-15)
- [fixed] 2026-05-13 hooks/session-start.sh ~145-177 — fi_target_is_v14x_broken awk programs are duplicated verbatim from bin/found-issues. The hook cannot source the binary (would re-trigger SessionStart logic); the duplication is structural. Drift risk: if discriminator logic changes in bin/found-issues, the hook silently diverges (suggested: add a 'keep in sync with fi_target_is_v14x_broken in bin/found-issues' comment ABOVE each awk block; OR extract the awk programs into lib/v14x-detect.sh and source from both call sites) (PR: AltDoug/found-issues#92) (fixed: 2026-05-15)
- [fixed] 2026-05-13 bin/found-issues Node shim ~2285 and Python shim ~2495 — Windows bash -c '"${posixPath}" status --format=segment' has no internal escaping for paths containing double quotes. Plugin cache paths from Claude Code's installer don't contain quotes today, so the failure mode is theoretical, but a path with a literal '"' would break the command (suggested: replace inline interpolation with execFileSync('bash', ['-c', '"$1" status --format=segment', '--', posixPath]) — pass the path as an argv arg so shell parsing doesn't see it; OR escape '"' to '\"' in posixPath before substituting) (PR: AltDoug/found-issues#92) (fixed: 2026-05-15)
- [open] 2026-05-15 lib/parse-entries.sh:91 — fi_parse_entry's location regex requires the path token to stand alone before ` — `; entries that include a function/symbol name and a `~line-range` location (e.g. `bin/found-issues fi_strip_target_markers ~1982-1989 — …`) get an empty `path=` result. This silently breaks annotate-pr/annotate-commit matching — those entries never get auto-flipped to [fixed] on PR merge. Discovered while annotating PR #92 against five 2026-05-13 entries; all five required manual sed annotation. (suggested: when location_part contains whitespace, take the first whitespace-separated token as the path and treat the remainder as supplementary location info; OR formalize the supported entry-location grammar in docs and lint entries that deviate. Either way add a parse-entries test case covering the `path symbol ~Nrange` form.)
