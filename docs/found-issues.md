# found-issues

Issues noticed outside the current task scope. Format: `- [status] YYYY-MM-DD path:line — symptom (suggested: fix)`. Statuses: `open`, `deferred`, `fixed`.

Maintained automatically by Claude. See <https://github.com/AltDoug/found-issues> for docs.

- [deferred] 2026-05-10 bin/found-issues:1166 cmd_install_statusline — has no awareness of whether ~/.claude/statusline.sh is the canonical machine file or a render target of an external sync system (e.g. dotfiles, chezmoi, yadm, a config sync layer). When it's a render target, install-statusline succeeds but the next sync overwrites the canonical block — silent regression. Hit during v1.0.4 dogfood: a config-management repo renders claude-only/statusline.sh to ~/.claude/statusline.sh, so every install-statusline migration got reverted on next config sync. Public users without a sync layer don't hit this, but the failure mode is invisible to the maintainer (counter just stops working again, no signal that an external system is fighting the install). (suggested: install-statusline could detect common render-target signals — symlink target outside HOME, sibling .gitignore'd file with same name, presence of a sync manifest in HOME — and emit a one-line warning at the end: 'note: ~/.claude/statusline.sh appears to be managed by <X>; if it gets reverted on next sync, add `found-issues install-statusline` as a post-render hook in your sync flow'. Low priority — only matters for power users with config management. Defer until second user reports the regression.) (reason: wait for second user report per entry guidance)
- [open] 2026-06-09 bin/found-issues:1972 — fi_strip_target_markers supports node/python only, so bash custom-target installs (`install-statusline --target foo.sh`) with a v1.5.0–v1.5.5 cd-only block have no rewrite path: cmd_install_statusline_target_bash no-ops on any existing marker block, leaving the --cwd-less (CLAUDE_PROJECT_DIR-vulnerable) segment in place forever. Canonical ~/.claude/statusline.sh installs are unaffected (they migrate via cmd_uninstall_statusline + reinstall in v1.5.6). (suggested: add bash case to fi_strip_target_markers — markers are identical to python's; the splice trailer is `# found-issues:seg` and the splice sub-pattern is `$__FI_SEG` — then wire fi_target_is_v15x_broken into cmd_install_statusline_target_bash like the node/python handlers.)
- [open] 2026-07-09 bin/found-issues:2430 — fi_strip_target_markers ran in place on the live target BEFORE the dry-run mode check and BEFORE the backup copy in the node/python migration branches: a default dry-run on a v1.4.x/v1.5.x target permanently stripped the existing marker block with no backup, and in apply mode the timestamped backup captured the already-stripped file — a re-splice failure after the strip left the user with no integration and no original to restore (suggested: strip a scratch copy and leave the target untouched until the apply-mode atomic rename; dry-run diffs original vs migrated)
- [open] 2026-07-09 tests/contract-segment.bats:101 — 12 tests hardcoded fixture entry dates that crossed the 30-day stale threshold ~2026-06-12 (5 segment-contract byte-snapshots, cli-status solo-residual label, autosync label, 5 custom-target runtime e2e): the stale counter appears and the residual label flips from issue(s) to other, so the full suite was red on all 3 platforms for ~1 month while no bats CI ran on main (last run 2026-06-09, PR #100) (suggested: dynamic date +%Y-%m-%d in freshness-dependent fixtures; weekly scheduled CI run so time rot on an idle main surfaces within a week)
- [open] 2026-07-09 hooks/session-start.sh:68 — canonical-statusline self-heal nudge classifier drifted from fi_statusline_state: v1.5.6 added a --cwd requirement for installed-fixed in the CLI but the hook inline classifier still only greps __FI_DIR, so v1.0.2–v1.5.5 cd-only canonical blocks classify as fixed in the hook and the daily self-heal nudge never fires — the v1.5.6 changelog claim that existing installs migrate automatically is dead for users who never manually re-run install-statusline (suggested: add the --cwd check to the hook marker_block_fixed detection to match fi_statusline_state, update the nudge kind wording, and extend the cli-statusline.bats hook-sync test that was supposed to catch this drift)
- [open] 2026-07-09 hooks/session-start.sh:140 — SessionStart custom-target auto-migration only detects v1.4.x POSIX-only blocks in node/python targets: no v1.5.x --cwd-less discriminator is wired in, and bash targets (*.sh) are excluded from the language case entirely, so v1.5.0–v1.5.5 custom-target installs never self-heal at session start and only migrate when the user manually re-runs install-statusline --target (suggested: add v1.5.x has-segment-without-cwd awk discriminators for node/python/bash mirroring fi_target_is_v15x_broken, and add *.sh|*.bash to the language case now that the CLI has a bash rewrite path as of v1.5.7)
