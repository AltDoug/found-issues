# found-issues

Issues noticed outside the current task scope. Format: `- [status] YYYY-MM-DD path:line — symptom (suggested: fix)`. Statuses: `open`, `deferred`, `fixed`.

Maintained automatically by Claude. See <https://github.com/AltDoug/found-issues> for docs.

- [fixed] 2026-05-10 bin/found-issues:1166 cmd_install_statusline — has no awareness of whether ~/.claude/statusline.sh is the canonical machine file or a render target of an external sync system (e.g. dotfiles, chezmoi, yadm, a config sync layer). When it's a render target, install-statusline succeeds but the next sync overwrites the canonical block — silent regression. Hit during v1.0.4 dogfood: a config-management repo renders claude-only/statusline.sh to ~/.claude/statusline.sh, so every install-statusline migration got reverted on next config sync. Public users without a sync layer don't hit this, but the failure mode is invisible to the maintainer (counter just stops working again, no signal that an external system is fighting the install). (suggested: install-statusline could detect common render-target signals — symlink target outside HOME, sibling .gitignore'd file with same name, presence of a sync manifest in HOME — and emit a one-line warning at the end: 'note: ~/.claude/statusline.sh appears to be managed by <X>; if it gets reverted on next sync, add `found-issues install-statusline` as a post-render hook in your sync flow'. Low priority — only matters for power users with config management. Defer until second user reports the regression.) (reason: wait for second user report per entry guidance) — CLOSED 2026-07-10 as won't-do (operator decision): the revisit trigger (second user report) never fired in 2 months and the operator's own render-target case is mitigated by agent-config post-sync hooks re-applying install-statusline; re-log on the first real user report. (verified: review) (fixed: 2026-07-10)
- [fixed] 2026-06-09 bin/found-issues:1972 — fi_strip_target_markers supports node/python only, so bash custom-target installs (`install-statusline --target foo.sh`) with a v1.5.0–v1.5.5 cd-only block have no rewrite path: cmd_install_statusline_target_bash no-ops on any existing marker block, leaving the --cwd-less (CLAUDE_PROJECT_DIR-vulnerable) segment in place forever. Canonical ~/.claude/statusline.sh installs are unaffected (they migrate via cmd_uninstall_statusline + reinstall in v1.5.6). (suggested: add bash case to fi_strip_target_markers — markers are identical to python's; the splice trailer is `# found-issues:seg` and the splice sub-pattern is `$__FI_SEG` — then wire fi_target_is_v15x_broken into cmd_install_statusline_target_bash like the node/python handlers.) (PR: AltDoug/found-issues#102) (fixed: 2026-07-09)
- [fixed] 2026-07-09 bin/found-issues:2430 — fi_strip_target_markers ran in place on the live target BEFORE the dry-run mode check and BEFORE the backup copy in the node/python migration branches: a default dry-run on a v1.4.x/v1.5.x target permanently stripped the existing marker block with no backup, and in apply mode the timestamped backup captured the already-stripped file — a re-splice failure after the strip left the user with no integration and no original to restore (suggested: strip a scratch copy and leave the target untouched until the apply-mode atomic rename; dry-run diffs original vs migrated) (PR: AltDoug/found-issues#102) (fixed: 2026-07-09)
- [fixed] 2026-07-09 tests/contract-segment.bats:101 — 12 tests hardcoded fixture entry dates that crossed the 30-day stale threshold ~2026-06-12 (5 segment-contract byte-snapshots, cli-status solo-residual label, autosync label, 5 custom-target runtime e2e): the stale counter appears and the residual label flips from issue(s) to other, so the full suite was red on all 3 platforms for ~1 month while no bats CI ran on main (last run 2026-06-09, PR #100) (suggested: dynamic date +%Y-%m-%d in freshness-dependent fixtures; weekly scheduled CI run so time rot on an idle main surfaces within a week) (PR: AltDoug/found-issues#102) (fixed: 2026-07-09)
- [fixed] 2026-07-09 hooks/session-start.sh:68 — canonical-statusline self-heal nudge classifier drifted from fi_statusline_state: v1.5.6 added a --cwd requirement for installed-fixed in the CLI but the hook inline classifier still only greps __FI_DIR, so v1.0.2–v1.5.5 cd-only canonical blocks classify as fixed in the hook and the daily self-heal nudge never fires — the v1.5.6 changelog claim that existing installs migrate automatically is dead for users who never manually re-run install-statusline (suggested: add the --cwd check to the hook marker_block_fixed detection to match fi_statusline_state, update the nudge kind wording, and extend the cli-statusline.bats hook-sync test that was supposed to catch this drift) (PR: AltDoug/found-issues#106) (fixed: 2026-07-10)
- [fixed] 2026-07-09 hooks/session-start.sh:140 — SessionStart custom-target auto-migration only detects v1.4.x POSIX-only blocks in node/python targets: no v1.5.x --cwd-less discriminator is wired in, and bash targets (*.sh) are excluded from the language case entirely, so v1.5.0–v1.5.5 custom-target installs never self-heal at session start and only migrate when the user manually re-runs install-statusline --target (suggested: add v1.5.x has-segment-without-cwd awk discriminators for node/python/bash mirroring fi_target_is_v15x_broken, and add *.sh|*.bash to the language case now that the CLI has a bash rewrite path as of v1.5.7) (PR: AltDoug/found-issues#106) (fixed: 2026-07-10)
- [fixed] [!] 2026-07-09 bin/found-issues:1727 cmd_archive — grep -F -v without -x removes archived lines by SUBSTRING match: an archived line that is a prefix of a newer non-archivable entry (structurally producible — sync appends "(fixed: date)" to the original line) deletes that newer entry from the active file WITHOUT writing it to the archive; verified e2e: archive --days=30 reported "moved 1 entries" but destroyed 2 lines, one unrecoverable (suggested: use grep -F -x -v for whole-line match, or filter with awk exact-line lookup; add regression test with prefix-pair entries) (PR: AltDoug/found-issues#104) (fixed: 2026-07-09)
- [fixed] [!] 2026-07-09 bin/found-issues:1315 cmd_sync tombstone — filesystem paths are taken verbatim from the committed found-issues.md and fi_parse_entry permits ../ components, so a hostile repo entry like ../../../../etc/hosts:999999 makes sync stat and wc -l files OUTSIDE the repo (path traversal + line-count/existence oracle), and SessionStart auto-runs sync with no user action; verified e2e (suggested: reject paths containing .. components at parse or sync time, or realpath-confine full_path under repo_root before any filesystem probe) (PR: AltDoug/found-issues#104) (fixed: 2026-07-09)
- [fixed] 2026-07-09 bin/found-issues:1330 cmd_sync tombstone — wc -l undercounts files lacking a trailing newline (counts newline chars), so an entry pointing at the LAST line of such a file is falsely auto-flipped to [fixed] (closure: tombstone); verified e2e both directions with/without trailing newline (suggested: count lines with awk "END{print NR}" or grep -c "" which count final unterminated lines correctly) (PR: AltDoug/found-issues#106) (fixed: 2026-07-10)
- [fixed] 2026-07-09 bin/found-issues:880 cmd_status — residual computed as total_open - in_pr - critical double-subtracts entries that are BOTH critical and in-PR, making other plain open entries vanish from all rendered counters; verified: 1 critical+in-PR entry + 1 plain open renders "1 critical · 1 in PR" with issues=0 (suggested: count the critical∩in-PR overlap and add it back, or compute the residual by exclusion in a single awk pass over fi_entries) (PR: AltDoug/found-issues#106) (fixed: 2026-07-10)
- [fixed] 2026-07-09 lib/parse-entries.sh:306 fi_count_stale — greedy ^-\ \[open\].*\ date\ regex captures the LAST space-flanked date in the line, so ISO dates inside symptom text corrupt the stale count in both directions (fresh entry with old date in symptom counts stale; stale entry with fresh date in symptom is missed); reproduced on bash 3.2 and 5.x (suggested: anchor the capture to the status prefix like fi_parse_entry post-#84 does, e.g. match ^- \[open\]( \[!\])? then capture the immediately following date) (PR: AltDoug/found-issues#106) (fixed: 2026-07-10)
- [fixed] 2026-07-09 lib/parse-entries.sh:261 — fi_count_in_pr, fi_count_critical (:275), and fi_count_stale demoted-term (:325) raw-grep the file instead of using conflict-aware fi_entries, counting BOTH sides of a merge conflict: verified critical=2/in_pr=2 while total_open=0 on a conflicted file, contradicting the documented invariant at :209-212 (suggested: pipe fi_entries output into the counters instead of grepping the raw file) (PR: AltDoug/found-issues#106) (fixed: 2026-07-10)
- [fixed] 2026-07-09 bin/found-issues:426 cmd_log — location charset [^:[:space:]]+ accepts paths fi_parse_entry (lib/parse-entries.sh:95 charset [A-Za-z0-9_./-]) cannot parse, e.g. + in src/UIView+Ext.swift, so such entries round-trip with empty path: dedup silently fails (verified double-log), annotate-pr/annotate-commit never match, tombstone sync never fires (suggested: align the two charsets — widen the parse regex or validate/reject at log time with a clear error) (PR: AltDoug/found-issues#106) (fixed: 2026-07-10)
- [fixed] 2026-07-09 bin/found-issues:139 fi_repo_id — sed capture [^/.]+ truncates repo names containing dots: vercel/next.js -> vercel/next, org/my.dotted.repo -> org/my (same regex duplicated lib/detect-mode.sh:101); annotate-pr writes the truncated slug into (PR: ...) annotations and sync then queries the wrong repo (suggested: capture up to the optional trailing .git instead, e.g. strip a literal .git$ suffix first then take the last two path segments verbatim) (PR: AltDoug/found-issues#106) (fixed: 2026-07-10)
- [fixed] 2026-07-09 bin/found-issues:927 segment-autosync — dispatches ( bash -c "$_autosync_cmd" & ) where the default command is unquoted $0 sync: an install path containing a space (e.g. Windows C:/Users/John Doe/...) word-splits, exits 127 silently, and autosync never fires (same pattern hooks/post-pr-state.sh:80 with $sync_cmd); verified with spaced-path repro (suggested: pass the binary as an argv positional — bash -c  sync _ "$0" — matching the quoting discipline the Windows shims already use) (PR: AltDoug/found-issues#106) (fixed: 2026-07-10)
- [fixed] 2026-07-09 hooks/pre-branch-delete.sh:66 — command patterns miss three valid git delete forms: git branch --delete NAME (pattern 1 only matches -[dD]), git push --delete REMOTE NAME (pattern 2 hard-codes remote-before---delete order), and git push REMOTE -d NAME; all three verified to exit 0 with unpromoted [open] entries while git branch -D and git push origin --delete correctly block (suggested: normalize the command token stream before matching — extract branch args after any of -d/-D/--delete regardless of position) (PR: AltDoug/found-issues#106) (fixed: 2026-07-10)
- [fixed] 2026-07-09 hooks/pre-commit.sh:44 — claims "Same validation rules as hooks/format-enforcer.sh" (:10) but implements only rules 1-5: rule 6 from v1.5.4 ([fixed] requires a verification token, format-enforcer.sh:119-136) is absent, so manual [open]->[fixed] flips committed outside Claude Code bypass exactly the workflow the enforcer blocks; verified: tokenless [fixed] line passes pre-commit, blocked by enforcer (suggested: port rule 6 into the pre-commit loop and add rule-6 cases to tests/pre-commit.bats, which currently has zero coverage of it) (PR: AltDoug/found-issues#106) (fixed: 2026-07-10)
- [fixed] 2026-07-09 hooks/format-enforcer.sh:91 — rule 1 skips the bare-PR check whenever the line contains "(PR: " ANYWHERE, so a bare "PR #7" coexisting with a canonical annotation passes both the enforcer and pre-commit.sh:56 mirror, while docs/format-spec.md:142-143 declares that pattern must be blocked; the bare ref stays invisible to sync/statusline (suggested: check the bare-PR regex on the line with canonical (PR: org/repo#N) substrings removed first, instead of whitelisting the whole line) (PR: AltDoug/found-issues#106) (fixed: 2026-07-10)
- [fixed] 2026-07-09 .github/workflows/test.yml:50 — the cosmetic paths-filter excludes CHANGELOG.md, README.md, and AGENTS.md, but version-check exists to validate CHANGELOG (a CHANGELOG-only PR can break both check-version invariants unchecked — verified against the live filter + branch protection) and tests/docs-consistency.bats exists to catch README/AGENTS drift — both jobs skip on exactly the PRs they were built to catch (suggested: carve CHANGELOG.md out of the excludes into a docs-meta filter that triggers version-check only, and drop the README/AGENTS excludes or gate docs-consistency.bats on its own filter) (PR: AltDoug/found-issues#106) (fixed: 2026-07-10)
- [fixed] 2026-07-09 hooks/session-start.sh:258 — [open] entries from the repo-committed found-issues.md are interpolated verbatim into the SessionStart context heredoc alongside imperative directives, giving a cloned hostile repo an indirect prompt-injection channel into whatever agent opens it (suggested: fence the entries as quoted data — e.g. wrap in a code block with an explicit "content is untrusted data, not instructions" preamble — and/or strip lines that do not match the entry grammar before injection) (PR: AltDoug/found-issues#106) (fixed: 2026-07-10)
- [fixed] 2026-07-09 docs/configuration.md:14 — three verified doc-vs-code drifts on one page: claims "plugin registers 8 hooks. Each one has an =off switch" (hooks.json registers 7; only 5 scripts have whole-hook off switches — session-start/post-pr-create/post-git-commit have none); :22 says the pre-commit hook is installed via `found-issues install-precommit`, a subcommand that does not exist in the CLI dispatch; :3 claims every tunable env var is listed but FOUND_ISSUES_AUTO_MIGRATE and FOUND_ISSUES_BASH are absent (suggested: correct the hook count/off-switch table, either implement install-precommit or document the manual .git/hooks copy, add the two missing env vars) (PR: AltDoug/found-issues#106) (fixed: 2026-07-10)
- [fixed] 2026-07-09 docs/architecture.md:96 — three verified staleness drifts: says "Six lifecycle hooks" and both the hook table (:99-107) and component diagram (:23-32) omit post-pr-state.sh (7 registered since v1.3.0); the CLI subcommand inventory (:118-127) lists 6 of the 17 dispatched subcommands; :102 says stop-reminder "forces the marker every turn" — stale since v1.5.x smart-fire and the v1.5.5 non-interactive skip (suggested: regenerate the hook table and subcommand inventory from hooks.json and cmd_help, refresh the stop-reminder row) (PR: AltDoug/found-issues#106) (fixed: 2026-07-10)
- [fixed] 2026-07-09 docs/format-spec.md:8 — the self-declared authoritative spec has three verified drifts: the grammar/Optional-parts/regex sections omit five annotation forms the system writes or accepts ((closure: tombstone), (touched:), (defer-cycle:), (reason:), (mute-until:)); :158 defines the stale counter as date-only while fi_count_stale implements date-stale UNION demoted (PR-closed:/commit-stale:) regardless of age; :44 says [deferred] is "Set by: User edit" — stale since v1.0.5 added the defer subcommand as the canonical path (suggested: add the five annotation forms to the grammar + table + regex list, document the stale union rule, update the deferred set-by row) (PR: AltDoug/found-issues#106) (fixed: 2026-07-10)
- [fixed] 2026-07-09 docs/faq.md:110 — uninstall answer says /plugin uninstall found-issues alone "removes hooks, removes the CLI from PATH, and deletes the plugin data directory" and never mentions the required /found-issues:uninstall first step — contradicting AGENTS.md:112-144 and README.md:116-124 ("Order matters") and re-creating the exact v1.0.3 leftover-statusline failure the two-step order exists to prevent (suggested: rewrite the FAQ answer to the ordered two-step and state what each step removes) (PR: AltDoug/found-issues#106) (fixed: 2026-07-10)
- [fixed] 2026-07-09 AGENTS.md:58 — claims "There is intentionally no auto-firing onboarding at session start... Setup is explicit, opt-in", but session-start.sh:26-39 auto-fires a first-run onboarding directive (prepend italic /found-issues:setup line) plus a daily statusline-nudge variant (:50-108); the sentence was true at v0.1.5 and drifted when v0.1.10 re-added the hint (suggested: rewrite the paragraph to describe the actual one-line first-run hint + daily nudge design and its rationale) (PR: AltDoug/found-issues#106) (fixed: 2026-07-10)
- [fixed] 2026-07-09 skills/rules/SKILL.md:73 — two verified overclaims in agent-facing rules: :71-74 says "Direct writes will be blocked by the format-enforcer hook" but well-formed direct writes pass in every mode (enforcer validates line format only — wholesale [open]-entry deletion and hand-typed (verified: ai) tokens pass); :122-125 says tombstone sync auto-closes when a "function [is] removed" but cmd_sync only checks file existence and line count — function-removal detection is assigned to the AI phase in commands/sync.md:68-71 (suggested: qualify both claims to match actual enforcement scope) (PR: AltDoug/found-issues#106) (fixed: 2026-07-10)
- [fixed] 2026-07-09 docs/statusline-integration-contract.md:18 — frozen-contract label policy says the residual relabels to "other" only when >=2 non-residual buckets are present, but the code (bin/found-issues:944-950 plain, :975-981 segment) relabels at >=1 — wrong from birth (PR #88 doc vs PR #62 code) and locked in by the contract-segment byte snapshots (suggested: one-word doc fix, >=2 -> any) (PR: AltDoug/found-issues#106) (fixed: 2026-07-10)
- [fixed] 2026-07-09 bin/found-issues:2186 fi_find_bash_splice_point — when a bash target has no LINE1= assignment, ALL echo lines are treated as splice points (multi-branch design), including user-authored echo lines that already reference ${__FI_SEG}; found during v1.5.7 verify: a hand-added "echo "custom ${__FI_SEG}"" line gets a second installer splice appended and renders the counter twice on that line (suggested: skip echo/printf lines that already contain __FI_SEG when building the splice set — they are user placements, not un-instrumented branches) (PR: AltDoug/found-issues#106) (fixed: 2026-07-10)
- [fixed] 2026-07-09 bin/found-issues:1030 cmd_annotate_pr — matches [open] entries to a PR by touched-file paths only, so a PR touching a hot file (bin/found-issues has 10+ open audit entries citing it) annotates every entry on that file regardless of whether the PR addresses it; on merge, sync auto-flips them all to [fixed] — silent false closures. Hit in production 2026-07-09: annotate-pr 102 tagged 12 entries of which only 3 were actually fixed; 9 required manual de-annotation (suggested: when multiple entries match one file, require symptom-level confirmation — emit the candidate list and let the invoking AI pick, or match on line-range overlap with the PR diff hunks instead of bare file paths) (PR: AltDoug/found-issues#106) (fixed: 2026-07-10)
- [fixed] 2026-07-10 (loc-validator) skills/rules/SKILL.md:204 (SKILL.md target 80, signal 150) — exceeds §12 refactor signal. (suggested: split into focused modules; or add a 'loc-override: <reason>' marker to the file per §12 cohesion carve-out.) (PR: AltDoug/found-issues#106) (fixed: 2026-07-10)
- [fixed] 2026-07-10 bin/found-issues:220 — cmd_help advertises "(Use /fi log inside Claude Code)" unconditionally, but the /fi alias is opt-in (cmd_install_fi_alias); users without it get a dead hint (suggested: reference /found-issues:log in help, mention /fi only in install-fi-alias output) (PR: AltDoug/found-issues#109) (fixed: 2026-07-10)
- [fixed] 2026-07-10 bin/found-issues:3504 — cmd_doctor runtime probe hand-inlines backslash+quote JSON escaping that diverges from fi_json_escape (no tab escape, no CR strip, no control-char handling); a workspace path containing a tab yields malformed probe stdin and a false-FAIL doctor verdict — the exact class the inline comment says it prevents (suggested: build probe_cwd_json via fi_json_str from lib/parse-entries.sh, which the CLI already sources) (PR: AltDoug/found-issues#109) (fixed: 2026-07-10)
- [fixed] 2026-07-10 bin/found-issues:2606 cmd_install_statusline_target_bash — spliced bash segment block goes silent under a hostile env: with a valid stdin workspace.current_dir and the CLI on PATH, setting CLAUDE_PROJECT_DIR=/nonexistent suppresses the segment entirely (verify-skill probe matrix 2026-07-10: normal env renders "1 issue"; hostile env renders host line only), contradicting the shim comment that stdin-cwd/--cwd is load-bearing and wins (suggested: diagnose which part of the spliced block consults CLAUDE_PROJECT_DIR before the stdin-derived __FI_DIR, make stdin cwd win outright, and add a hostile-env case to the custom-target e2e bats) (PR: AltDoug/found-issues#109) (fixed: 2026-07-10)
- [fixed] 2026-07-10 tests/helpers.bash:36 fi_teardown_tmp — single-shot rm -rf races the background segment-autosync process on Windows: the dispatched $0 sync child can still hold the temp dir as cwd when teardown fires, failing the whole test with "Device or resource busy" (observed: PR #109 Windows run, test 253 body passed, teardown failed; #108 run green — flake) (suggested: bounded retry in fi_teardown_tmp — 3 attempts with 1s sleep — since Windows releases the dir as soon as the child exits) (PR: AltDoug/found-issues#109) (fixed: 2026-07-10)
- [open] 2026-07-19 docs/configuration.md:14 — hook count says 7, now 5 after post-bash-dispatch consolidation (also stale at docs/architecture.md:97, both text and hook table); FOUND_ISSUES_AUTO_ANNOTATE/_MAX undocumented (suggested: refresh both pages' hook counts and add the new env-var rows, likely bundled with the architecture.md rewrite already scheduled for a later codex-compat-usage-diet task) (commit: 57a33ef)
- [open] 2026-07-19 hooks/session-start.sh:296 — critical detection greps [!] anywhere in the entry line, so a symptom containing literal [!] text is over-included as critical by the injection cap (suggested: anchor to the status prefix, e.g. grep -E '^- \[open\] \[!\] ') (commit: df09dc6)
- [open] 2026-07-20 commands/doctor.md:42 — See-also references /found-issues:doctor-statusline, a slash command that does not exist (no commands/doctor-statusline.md; the entry links to a bare (#) anchor, so it was already inert). Task 7's codex generator faithfully carries the dangling reference into codex-skills/fi-doctor/SKILL.md as fi-doctor-statusline (suggested: either add commands/doctor-statusline.md or drop/soften the See-also line) (commit: 57a33ef)
- [deferred] 2026-07-20 hooks/stop-reminder.sh:46 — stop-hook marker discipline is fail-open on Codex v1: transcript rollout format is unparsed, so the marker convention is Claude-only (suggested: port smart-fire transcript parsing to the Codex rollout format) (reason: v1 design decision — Codex port of smart-fire is a 2.x follow-up, documented in AGENTS.md/FAQ)
- [open] 2026-07-20 docs/versioning.md:10,85 — release checklist references a nonexistent "## Status & roadmap" header in README.md (suggested: update to "## Status" or rename the README header to match)
- [open] 2026-07-20 bin/found-issues:426 cmd_log — location line spec accepts non-numeric forms like 10,85 that fi_parse_entry cannot round-trip: the entry parses as abstract/pathless, so dedup, annotate-commit/annotate-pr matching, and tombstone sync all silently miss it — same charset-alignment class as the v1.5.7 path fix, now via the line field (suggested: validate the post-colon line spec as ^[0-9]+$ at log time and reject with exit 2 and a clear message) (commit: 769cc01)
