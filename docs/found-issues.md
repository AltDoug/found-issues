# found-issues

Issues noticed outside the current task scope. Format: `- [status] YYYY-MM-DD path:line — symptom (suggested: fix)`. Statuses: `open`, `deferred`, `fixed`.

Maintained automatically by Claude. See <https://github.com/AltDoug/found-issues> for docs.

- [fixed] 2026-07-19 docs/configuration.md:14 — hook count says 7, now 5 after post-bash-dispatch consolidation (also stale at docs/architecture.md:97, both text and hook table); FOUND_ISSUES_AUTO_ANNOTATE/_MAX undocumented (suggested: refresh both pages' hook counts and add the new env-var rows, likely bundled with the architecture.md rewrite already scheduled for a later codex-compat-usage-diet task) (commit: 57a33ef) (PR: AltDoug/found-issues#114) (fixed: 2026-07-20)
- [fixed] 2026-07-19 hooks/session-start.sh:296 — critical detection greps [!] anywhere in the entry line, so a symptom containing literal [!] text is over-included as critical by the injection cap (suggested: anchor to the status prefix, e.g. grep -E '^- \[open\] \[!\] ') (commit: df09dc6) (PR: AltDoug/found-issues#114) (fixed: 2026-07-20)
- [fixed] 2026-07-20 commands/doctor.md:42 — See-also references /found-issues:doctor-statusline, a slash command that does not exist (no commands/doctor-statusline.md; the entry links to a bare (#) anchor, so it was already inert). Task 7's codex generator faithfully carries the dangling reference into codex-skills/fi-doctor/SKILL.md as fi-doctor-statusline (suggested: either add commands/doctor-statusline.md or drop/soften the See-also line) (commit: 57a33ef) (PR: AltDoug/found-issues#114) (fixed: 2026-07-20)
- [deferred] 2026-07-20 hooks/stop-reminder.sh:46 — stop-hook marker discipline is fail-open on Codex v1: transcript rollout format is unparsed, so the marker convention is Claude-only (suggested: port smart-fire transcript parsing to the Codex rollout format) (reason: v1 design decision — Codex port of smart-fire is a 2.x follow-up, documented in AGENTS.md/FAQ)
- [fixed] 2026-07-20 docs/versioning.md:10,85 — release checklist references a nonexistent "## Status & roadmap" header in README.md (suggested: update to "## Status" or rename the README header to match) (verified: ai) (fixed: 2026-07-20)
- [fixed] 2026-07-20 bin/found-issues:426 cmd_log — location line spec accepts non-numeric forms like 10,85 that fi_parse_entry cannot round-trip: the entry parses as abstract/pathless, so dedup, annotate-commit/annotate-pr matching, and tombstone sync all silently miss it — same charset-alignment class as the v1.5.7 path fix, now via the line field (suggested: validate the post-colon line spec as ^[0-9]+$ at log time and reject with exit 2 and a clear message) (commit: 769cc01) (PR: AltDoug/found-issues#114) (fixed: 2026-07-20)
- [fixed] [!] 2026-07-20 .codex-plugin/plugin.json:14 — Codex CLI 0.144.5 has REMOVED the plugin_hooks feature (`codex features list` → `plugin_hooks  removed  false`; force-enabling leaves it removed/false), so a plugin's "hooks" manifest pointer is inert: the found-issues SessionStart rules injection, PostToolUse annotation, and Stop marker never load on Codex — only skills load (verified via headless codex exec: fi-* skills visible, rules/ledger absent across 3 runs, zero hook-subsystem trace activity). Invalidates spec B2/B3/B5 Codex-hooks assumptions (suggested: to ship hooks on Codex today, install them into $CODEX_HOME/hooks.json via the stable `hooks` feature during setup rather than the plugin manifest; or gate the Codex-hooks work behind plugin_hooks returning; update AGENTS.md/docs to state hooks are Claude-only on Codex 0.144.5) (commit: 08eacba) (PR: AltDoug/found-issues#114) (fixed: 2026-07-20)
- [fixed] 2026-07-20 hooks/session-start.sh:37 — Codex branch injects the rules block as PLAIN-TEXT stdout, but Codex's session-start.command.output JSON Schema expects additionalContext nested under hookSpecificOutput (like the PostToolUse shape fixed in 4c8b711); unverified whether Codex accepts plain stdout as context (blocked in Task 11: plugin_hooks removed + hook-trust wall). If SessionStart hooks are ever wired via $CODEX_HOME/hooks.json, plain text likely will NOT inject (suggested: once verifiable via --dangerously-bypass-hook-trust, emit the JSON envelope on Codex for SessionStart too; keep shape isolated like fi_emit_post_context) (commit: 08eacba) (PR: AltDoug/found-issues#114) (fixed: 2026-07-20)
- [fixed] 2026-07-20 AGENTS.md:73 — Codex install/uninstall docs use `codex plugin install/uninstall`, but codex-cli 0.144.5 subcommands are `codex plugin add` / `codex plugin remove` (no install/uninstall exist); marketplace add of a local checkout needs a .agents/plugins/marketplace.json manifest (schema: name + interface.displayName + plugins[].source{source:local,path} + policy + category) (suggested: s/plugin install/plugin add/, s/plugin uninstall/plugin remove/, document the marketplace.json requirement for local installs) (commit: 08eacba) (PR: AltDoug/found-issues#114) (fixed: 2026-07-20)
- [fixed] 2026-07-20 README.md:11 — stat strip claims "618 tests" but the suite is at 641 (already 623 at this task's baseline, pre-dating this session's additions); drifts every time tests are added without a matching README edit (suggested: either update the number as part of test-adding PRs or replace the literal count with a testscount badge/CI-generated value so it can't drift) (commit: 97bbcb9) (PR: AltDoug/found-issues#114) (fixed: 2026-07-20)
- [fixed] 2026-07-20 bin/found-issues:4368 fi_codex_hooks_new_entries_json — installed Codex hook commands embed the install root unquoted ("env FOUND_ISSUES_HARNESS=codex $hooks_dir/session-start.sh"); an install path containing a space (same class as the fixed bin/found-issues:927 segment-autosync bug) would break however Codex parses hooks.json's command string (unverified whether Codex shell-splits or execs argv directly — Task 11 never exercised a spaced path) (suggested: once Codex's command-string parsing is confirmed, quote the resolved script path, e.g. env FOUND_ISSUES_HARNESS=codex "$hooks_dir/session-start.sh") (commit: 97bbcb9) (PR: AltDoug/found-issues#114) (fixed: 2026-07-20)
- [fixed] 2026-07-20 bin/found-issues:3733 — doctor runtime sub-probe (b): grep -c prints 0 AND exits 1 on no match, so `|| echo 0` yields a two-line "0\n0" value and the (( seg_count < output_count )) comparison at :3734 emits "arithmetic syntax error" noise into doctor output (suggested: drop the || echo 0 fallbacks or use ${var:-0} after stripping; grep -c already prints 0) (commit: 902e100) (PR: AltDoug/found-issues#114) (fixed: 2026-07-20)
- [fixed] 2026-07-20 (loc-validator) codex-skills/fi-setup/SKILL.md:339 (SKILL.md target 80, signal 150) — exceeds §12 refactor signal. (suggested: split into focused modules; or add a 'loc-override: <reason>' marker to the file per §12 cohesion carve-out.) (commit: 008b891) (PR: AltDoug/found-issues#114) (fixed: 2026-07-20)
- [fixed] 2026-07-20 tests/session-start.bats:27 — six vacuous mid-test negations (lines 27,53,76,166,241,242): bare '! grep/! ls' forms are exempt from bats errexit so they never assert; pre-existing on main, untouched by the v2 branch (suggested: convert each to run + [ "$status" -ne 0 ], same as the cli-annotate-hook-auto fix) (commit: fd98b1f) (PR: AltDoug/found-issues#114) (fixed: 2026-07-20)
- [deferred] 2026-07-20 docs/found-issues.md:53 — loc-validator hook entries put a (loc-validator) tag before the path, so fi_parse_entry takes the literal (loc-validator) as the path with empty line and the shared generic symptom; two simultaneously-open loc-validator entries would be ambiguous to --pick and could false-collapse under dedup since path+line+symptom are identical across different target files (suggested: teach the loc-validator hook to emit path:line first with the tag after, or teach fi_parse_entry to skip a leading parenthesized tag) (reason: v2.0.x follow-up — parser tolerance for loc-validator tag entries)
- [deferred] 2026-07-20 bin/found-issues:3650 — numeric-or-zero guard for grep -c/wc -l counts is duplicated inline at 8+ sites (1042, 1894, 1991, 3651, 3770, 4056, 4064, 4066, 4107) with two drifted spellings ([[ =~ ]] || var=0 vs [[ -z || ! =~ ]] && var=0) and a tr -d whitespace workaround present at only 3 of them (suggested: extract a shared fi_to_int helper in the Utilities section and converge all sites on it) (reason: v2.0.x follow-up — fi_to_int consolidation across 8+ sites)
- [deferred] 2026-07-20 tests/helpers.bash:62 — no negation assertion helper: the run + [ "$status" -ne 0 ] two-liner is hand-rolled 34+ times across 13 test files (grew by 6 in the vacuous-negation fix); any tweak to the idiom (e.g. -- before patterns) means touching every site (suggested: add fi_refute_match/fi_refute_exists helpers next to fi_run and convert call sites opportunistically) (reason: v2.0.x follow-up — fi_refute_match helper, convert call sites opportunistically)
- [deferred] 2026-07-20 hooks/post-bash-dispatch.sh:232 — FI_BIN resolution prefers any PATH found-issues over the co-located/plugin binary, so a version-skewed older CLI (observed: marketplace 1.7.1 on PATH while hooks are 2.0.0) rejects --hook-auto with rc=2 and the pr-create/git-commit routes silently swallow it (only rc 0 + Annotated and rc 3 are handled) — auto-annotation dies with zero signal to the model or user (suggested: on rc 2 fall back to the legacy prompt path, or verify the resolved CLI supports --hook-auto e.g. version >= 2 before routing, preferring CLAUDE_PLUGIN_ROOT over PATH) (reason: v2.0.1 candidate — real bug, needs TDD fix for rc=2 fallback; skew self-resolves on this machine once marketplace 2.0.0 installs)
- [fixed] 2026-08-11 bin/found-issues:4811 (script (by shebang) target/signal 500) — loc-validator: exceeds §12 refactor signal. (suggested: split into focused modules; or add a 'loc-override: <reason>' marker to the file per §12 cohesion carve-out.) (PR: AltDoug/found-issues#150) (fixed: 2026-08-13)
- [fixed] [!] 2026-08-11 bin/found-issues:745 — three shipped commands silently DROP the ledger's last entry when it lacks a trailing newline: defer, sync and annotate-commit all rewrite via 'while IFS= read -r line' without the '|| [[ -n $line ]]' guard, so read returns non-zero on a final partial line and the body never runs for it. Verified by probe: 2-entry ledger goes to 1 for each. sync is the worst case since SessionStart runs it automatically, so the loss happens with no user action. resolve (v2.2.0) is guarded; the other ~10 read-loop sites need auditing (suggested: add '|| [[ -n $line ]]' to every ledger-rewriting read loop, or extract a shared fi_rewrite_ledger helper so the idiom exists in exactly one place) (PR: AltDoug/found-issues#129) (fixed: 2026-08-11)
- [fixed] 2026-08-11 hooks/session-start.sh:196 — FI_BIN resolution prefers any 'found-issues' on PATH over the co-located FI_BIN_DIR binary, so running the hook from a source checkout silently executes the INSTALLED plugin version instead of the working tree; a local hook/CLI change appears to have no effect (or a fixed bug appears unfixed), with no output naming which binary ran (suggested: log the resolved FI_BIN path under a debug env var, or prefer the co-located binary when the hook lives inside a git checkout that contains it) (PR: AltDoug/found-issues#132) (fixed: 2026-08-11)
- [fixed] [!] 2026-08-11 .github/workflows/test.yml:32 — a docs-only push landing on main shortly after a code merge silently voids the code's multi-OS verification: concurrency cancel-in-progress kills the in-flight code matrix, and the replacement run skips bats/shellcheck/version-check via the changes filter (code=false) while the 'ci' aggregator still reports success, so main shows green having never run bats on macOS or Windows. Observed 2026-08-11: run 31512169066 (v2.2.1 code, e76071c) cancelled mid-matrix by run 31512362585 (ledger flip, all jobs skipped, ci=success) (suggested: make the aggregator treat skipped bats on a push to the default branch as a failure, or exclude docs-only pushes from the cancel-in-progress group so they cannot cancel a code run) (PR: AltDoug/found-issues#132) (fixed: 2026-08-11)
- [fixed] 2026-08-11 lib/parse-entries.sh:118 — fi_parse_entry accepts only ^[0-9]+$ as a line spec, so LINE-RANGE locations (path:23-49) parse to path='' line='': the entry is then invisible to annotate-pr/annotate-commit (both --pick and auto-match need a path, so --pick reports 'no [open] entry matches' for an entry plainly present, reading as user error rather than a parser limit) and is never tombstone-checked by sync. cmd_log now REJECTS range specs, so this only affects entries logged before that guard; they are unreachable by every annotate path. Confirmed 2026-08-11 by probe and reported independently by the orchard session (6 of 11 orchard entries are range-form). NOT affected: PR/commit annotation flips still work, since fi_parse_entry emits prs=/commits= from the annotation tail and cmd_sync:2024 gates the flip on $e_prs, not $e_path (suggested: accept ^[0-9]+(-[0-9]+)?$ in re_path_line and keep the range verbatim in the line field, so path:23-49 round-trips as a location token with no on-disk format change) (commit: da1d10a) (PR: AltDoug/found-issues#137) (fixed: 2026-08-11)
- [fixed] 2026-08-11 bin/found-issues:4157 — doctor reports the resolved CLI as a bare '✓ CLI: <path>' without comparing it to the INSTALLED plugin version, so it shows green while the session runs a stale binary. Claude Code injects a version-pinned plugin bin dir into the environment at session start, so a session started before a release keeps resolving the old version indefinitely (installed_plugins.json installPath=2.2.2 while $PATH still had .../found-issues/2.2.0/bin). Verified 2026-08-11 across two concurrent sessions: both ran 2.2.0, including its SessionStart auto-sync, for hours after v2.2.1 shipped the read-loop data-loss fix — doctor would have called that healthy. Doctor is the one command whose entire job is catching this class (suggested: compare the resolved CLI's FI_VERSION against .claude-plugin/plugin.json version in the installPath from ~/.claude/plugins/installed_plugins.json, and WARN with 'restart your session to pick up vX.Y.Z' on mismatch; the version dirs accumulate rather than prune, which is why the stale path resolves quietly instead of failing loudly) (PR: AltDoug/found-issues#137) (fixed: 2026-08-11)
- [fixed] 2026-08-11 bin/found-issues:507 — cmd_log REJECTS line-range location specs (path:23-49) even though fi_parse_entry, fi_entry_loc and --pick now round-trip them as of v2.2.3 (#137): the guard predates range support, so an agent that spots a multi-line symptom must narrow it to one line or hand-edit the ledger -- and hand-edited entries are exactly the pre-guard shape v2.2.3 had to rescue. Not a regression (the guard has always behaved this way), but the parser and the writer now disagree about what a valid location is (suggested: accept ^[0-9]+(-[0-9]+)?$ in cmd_log's line-spec validation to match re_path_line, rejecting only inverted ranges where end < start) (PR: AltDoug/found-issues#141) (fixed: 2026-08-11)
- [fixed] 2026-08-11 bin/found-issues:1569 — fi_line_matched tests only a range entry's START line against the diff's old-side hunks, so the PostToolUse hook's stricter auto-annotate rule misses a line-range entry whose start sits outside every changed hunk while its body overlaps one (entry at path:23-49, hunk touching 30-40 -> no match). Introduced as a deliberate scope limit in v2.2.3 (#137), which split :23-49 into a numeric start plus line_end precisely so this and the other two arithmetic consumers stayed correct without change; overlap was left out as gold-plating. Fails SAFE (a missed auto-annotation, never a wrong one) and --pick is unaffected, so this is a reachability gap, not a correctness bug (suggested: pass line_end through to fi_line_matched and test hunk OVERLAP -- start <= re && end >= rs -- falling back to the start-only test when line_end is empty) (PR: AltDoug/found-issues#141) (fixed: 2026-08-11)
- [fixed] 2026-08-12 bin/found-issues:2215 — cmd_sync tombstones any [open] entry whose cited line exceeds the file's CURRENT line count, so a file that merely SHRANK false-closes its own still-valid entries. Hit live 2026-08-12 during the tracked §12 extraction: moving the statusline family out took bin/found-issues 5209 -> 3443 lines, a peer session's SessionStart auto-sync ran, and the loc-validator entry cited at :4811 flipped to [fixed] (closure: tombstone) while the file was still 3443 lines against a 500 signal — the exact entry the operator said may only be closed by the final extraction PR. Reproduced deterministically on a fixture (999-line file shrunk to 400, entry at :900) against BOTH 2.2.5 and origin/main 2.2.4, so it is pre-existing, not introduced by the extraction. Same false-close family as the rename check at :2199 and the awk END{NR} vs wc -l fix at :2211, which both exist to stop this heuristic firing on a file that is still present. Silent and unattended: SessionStart sync needs no user action and prints nothing about which entry it closed (suggested: only tombstone on line-count shortfall when the file also stopped matching the entry, e.g. require the file to be deleted/renamed as the :2199 branch already does, and treat a present-but-shorter file as line drift — re-point or leave [open] — rather than a closure; at minimum name the closed entries in sync's summary so an unattended flip is visible) (PR: AltDoug/found-issues#149) (fixed: 2026-08-12)
- [open] 2026-08-12 scripts/gen-codex-skills.sh:49 — the generator copies the Claude Code slash-command description verbatim into the Codex skill description, but the two surfaces have different jobs: a slash command is invoked BY NAME so its description is a menu label (one terse line is correct), while a Codex skill is MODEL-ROUTED so the description is the only thing the model chooses from. Result: 0 of 13 generated codex-skills descriptions carry any when-to-use / when-NOT-to-use, median length 115 chars — against 31 of 39 and median 613 chars for agent-config's hand-written skills, which land in the SAME ~/.agents/skills/ destination read by the same harness. Every verb here has a confusable sibling (log vs promote vs defer, annotate-pr vs annotate-commit, sync vs resolve) and the plugin's hard-won boundaries (--pick never --all; never hand-edit the ledger; only log/annotate-*/sync may write it) live in command bodies and in none of the routing text. Found by the 2026-08-12 prompt-audit (docs/audits/prompt-audit-2026-08-12.md finding 1, HIGH — Group 3 under-description, whose documented fix direction is ADD not trim). (suggested: add an optional second frontmatter key to commands/*.md (e.g. codex-description:) that gen-codex-skills.sh prefers when present and falls back to description: when absent — keeps the Claude Code picker label short AND gives Codex real routing text without desyncing the two, which is the property the generator exists to protect; fix at the generator, not in the 13 generated files (they are overwritten every run))
- [fixed] 2026-08-12 docs/versioning.md:7 — the release checklist cites 'FI_VERSION in bin/found-issues (line 9)' but the constant is at line 38 and has drifted with every header change (READ-LOOP GUARD block, loc-override marker, the v2.2.5/v2.2.6 source lines). A reader following the checklist lands in the middle of the fi_script_dir helper. Pre-existing drift, unrelated to the ongoing §12 split, but the split moves the line again each PR. Same class as docs/statusline-integration-contract.md:48, which cites 'line ~971' for the cmd_status segment branch that now sits at 1239 and moves to lib/ in the next extraction PR (suggested: drop the parenthetical line numbers from both docs and cite the symbol instead — 'the readonly FI_VERSION line', 'the segment branch of cmd_status' — since no test pins them and they have silently drifted through at least four releases) (PR: AltDoug/found-issues#155) (fixed: 2026-08-14)
- [open] 2026-08-12 bin/found-issues:1988 — annotate-commit defaults to HEAD (`[[ -z $target ]] && target="HEAD"`), and the closer at :2131 closes an entry on ANY annotated SHA that is an ancestor of the default branch — with no check that the commit relates to the entry. Together these false-close an entry when annotate-commit is run BEFORE the fix is committed: on a fresh branch cut from origin/main, HEAD is still main's tip, that SHA is trivially an ancestor, and the entry flips to [fixed] with a fix that never landed. This is not hypothetical — reproduced live 2026-08-12 in agent-config: `found-issues annotate-commit HEAD --pick agents/done-verifier.md:6` was run on branch fix/found-issues-20260812 before committing, HEAD resolved to 28f54ab (already in main, the PR that merely LOGGED the entry), and the entry closed as fixed: 2026-08-12 while the actual fix sat uncommitted. The branch-then-fix ordering that triggers it is exactly what /found-issues:fix Phase 3 prescribes ('Branch fix/found-issues-<YYYYMMDD> off the default branch. Never fix on the current or default branch'), so the workflow's own shape sets the trap. Compounding: there is no de-annotate command, and the ledger must not be hand-edited, so a mis-annotation is unrecoverable through supported means. (suggested: reject an annotate-commit target that is already an ancestor of the default branch unless --force is passed — a commit already on main cannot be the fix for a still-open entry logged against it; alternatively drop the HEAD default and require an explicit SHA. Either way also consider a de-annotate/undo path, since today a mis-annotation cannot be corrected through any supported command.) (PR: AltDoug/found-issues#156)
- [fixed] 2026-08-12 lib/resolve.sh:139 — four comments inside code moved to lib/ by the v2.2.5-v2.2.7 §12 split still point 'at the top of this file' for things that stayed in bin/found-issues: three cite the READ-LOOP GUARD convention block (lib/resolve.sh:139 and :171, lib/defer.sh:150) and one cites fi_script_dir (lib/codex-hooks.sh:80). Each is now a dangling reference — a reader in lib/resolve.sh scrolls to a header that says nothing about read loops, and the guard those comments justify is exactly the v2.2.1 data-loss class, so the reference matters. Left verbatim on purpose: the extraction PRs prove byte-identical moves and editing comment text inside the moved blocks would forfeit that proof for cosmetic gain (suggested: after the final extraction PR lands, retarget the four comments to 'the READ-LOOP GUARD block in bin/found-issues' and 'fi_script_dir in bin/found-issues' in one docs-only commit; tests/source-guards.bats already enforces the convention mechanically across lib/, so only the human-readable pointer is stale) (PR: AltDoug/found-issues#155) (fixed: 2026-08-14)
- [fixed] 2026-08-12 .github/workflows/test.yml:56 — the #132 concurrency fix protects RUNNING push-to-main runs but not PENDING ones, so the multi-OS-voiding failure it was written to stop just recurred. With cancel-in-progress false, GitHub still allows only ONE pending run per concurrency group: a newer arrival CANCELS the queued one. Observed 2026-08-12: run 31564662186 (v2.2.6 code) held the slot in_progress, run 31567113734 (v2.2.7 code, 4a16a9e) sat pending behind it, and the docs-only push 70fad3f (#147) arriving 2 min later cancelled the pending v2.2.7 run outright — zero jobs ran. The replacement run 31567218590 then skipped every bats job via the changes filter (docs-only) while ci reported success, so main showed green having NEVER run bats on macOS or Windows for v2.2.7. Identical net effect to the [fixed] entry at .github/workflows/test.yml:32 (PR #132), reached through the pending path instead of the in-progress path, so the existing fix and its 8-line rationale comment both read as complete when they are not. Recovered manually this session by gh run rerun 31567113734 (suggested: make the ci aggregator FAIL when bats is skipped on a push to the default branch whose merge-base introduced code changes, so a docs-only replacement run cannot report green for unverified code — the aggregator-side half of the #132 suggestion that was never implemented; alternatively give push-to-main runs a per-SHA concurrency group (group: ...-${{ github.sha }}) so back-to-back merges never share a queue slot at all) (PR: AltDoug/found-issues#154) (fixed: 2026-08-14)
- [open] 2026-08-14 lib/archive.sh:36 — four more mutating/user-facing subcommands still SWALLOW unknown flags via a `*) shift ;;` catch-all, the same defect fixed for sync in v2.3.0 (#151): cmd_archive (lib/archive.sh:36), cmd_promote (lib/promote.sh:96), cmd_install_statusline (lib/statusline-install.sh:93), cmd_uninstall_statusline (lib/statusline-uninstall.sh:26), plus the arg loop at bin/found-issues:298. archive is irreversible in the same way sync is — it MOVES entries out of the ledger into found-issues-archive.md — so `archive --help` runs a real archive pass and `archive --dry-runn` (typo) silently archives for real instead of erroring. The statusline pair rewrites the user settings.json. Scoped out of the v2.3.0 fix deliberately to keep that PR to the reported command (suggested: extract the reject-unknown-flag idiom from cmd_sync into a shared helper (e.g. fi_reject_unknown_flag <cmd> <arg>) and apply it to all five sites, adding -h/--help to each; archive already has --dry-run so it only needs the catch-all tightened)
- [fixed] 2026-08-14 docs/versioning.md:90 — release checklist step 8 instructs a manual `git tag -a vX.Y.Z ... && git push origin vX.Y.Z` after merge, but .github/workflows/release.yml (PR #113, 2026-07-10) auto-cuts the tag and GitHub Release whenever a merged main commit changes .claude-plugin/plugin.json — observed live on v2.3.0: release appeared ~1 min after the #153 merge with no manual step. A reader following the checklist either races the workflow or concludes it is broken; the memory-level correction (do NOT tag by hand, check `gh release list` first) never made it into the doc (suggested: rewrite step 8 to say the tag+release are cut automatically by release.yml and the step is to VERIFY via `gh release list --limit 1`, falling back to the manual command only if the workflow is disabled or failed) (PR: AltDoug/found-issues#155) (fixed: 2026-08-14)
- [open] 2026-08-14 lib/sync.sh:147 — residual of the annotate-commit false-close fix (v2.4.0): the ancestor guard in lib/annotate-commands.sh covers only the branch-cut entry point, and two premature-annotation shapes still arm the closer, which flips an entry on ANY annotated SHA reaching the default branch with no check that the commit relates to the entry. (1) On the default branch itself with an open entry, a bare annotate-commit run BEFORE committing the fix skips the guard (current branch == default branch is the deliberate exemption that keeps the direct-to-main and --hook-auto flows working), HEAD resolves to a stale tip, and the entry false-closes on the next sync while the fix sits uncommitted. (2) A fix branch cut off another UNMERGED feature branch and annotated before committing points at the parent branch tip — not yet an ancestor of main, so the guard is silent — and the entry false-closes the moment the parent merges. A mis-annotation also remains unrecoverable through supported means (no de-annotate command). Found by /code-review medium on the v2.4.0 diff (suggested: add a relate-commit-to-entry check at the closer — does the annotated SHA touch the entry cited files or postdate the entry — so an unrelated SHA can never close an entry regardless of how it was annotated; on the default branch, testing ancestry against origin/<default> would additionally catch unpushed stale-tip annotations; and consider the de-annotate/undo verb the original entry already asked for)
